// 🌐 Configuración de entorno para desarrollo (usada por defecto en `ng serve`).
// La build de producción sustituye este fichero por `environment.production.ts`
// mediante `fileReplacements` en angular.json.
export const environment = {
  production: false,
  apiRoot: 'https://wilds.mhdb.io'
};
